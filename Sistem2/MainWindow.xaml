<Window x:Class="Sistem2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:layerTypes="clr-namespace:Sistem2.LayerTypes"
        xmlns:sistem2="clr-namespace:Sistem2"
        mc:Ignorable="d"
        Title="MainWindow" Height="1280" Width="800" WindowState="Maximized">
	<DockPanel LastChildFill="True">
		<Menu DockPanel.Dock="Top">
			<MenuItem Header="_File">
			</MenuItem>
			<MenuItem Header="_Layers">
				<MenuItem Header="Add _image layer" Click="AddImageLayerClick"/>
				<MenuItem Header="Add _SIRDS layer" Click="AddSirdsLayerClick"/>
			</MenuItem>
			<MenuItem Header="_Draw" Click="DrawClick" />
		</Menu>
		<StackPanel Width="200" DockPanel.Dock="Left" x:Name="LayersStackPanel">
			<sistem2:ImageLayerProperties x:Name="ImageLayerProperties" Visibility="Collapsed" />
			<sistem2:BackgroundLayerProperties x:Name="BackgroundLayerProperties" Visibility="Collapsed" />
			<sistem2:SirdsLayerProperties x:Name="SirdsLayerProperties" Visibility="Collapsed" />
		</StackPanel>
		<StackPanel Width="220" DockPanel.Dock="Right">
			<ListBox Name="LayersListBox" ItemsSource="{Binding}" SelectionChanged="LayersListBoxSelectionChanged" d:DataContext="{d:DesignInstance layerTypes:LayerBase}">
				<ListBox.ItemTemplate>
					<DataTemplate>
						<StackPanel Orientation="Horizontal">
							<Image Source="{Binding Path=Preview}" Width="50" Height="50" />
							<CheckBox IsChecked="{Binding Path=Visible}" VerticalAlignment="Center" Margin="5"></CheckBox>
							<TextBox Text="{Binding Path=Name}" Height="24" Width="100"></TextBox>
							<Button IsEnabled="{Binding Path=IsDeleteEnabled}" Content="&#xf1f8;" FontFamily="{StaticResource FontAwesomeRegular}" FontSize="16" VerticalAlignment="Center" Margin="5" Click="DeleteLayerClick" />
						</StackPanel>
					</DataTemplate>
				</ListBox.ItemTemplate>
			</ListBox>
		</StackPanel>
		<sistem2:ZoomBorder x:Name="border" ClipToBounds="True" Background="Gray">
			<Image Margin="0,0,0,0" Name="PreviewImage"  />
		</sistem2:ZoomBorder>
	</DockPanel>
</Window>
